import React from 'react'

type Props = {}

const Content = (props: Props) => {
  return (
    <main className='landing-content'>
      <section className='landing-intro'>
        Flashy intro portion
      </section>
      <section className='landing-details'>
        Details portion
      </section>
    </main>
  )
}

export default Content